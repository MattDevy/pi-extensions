## 2026-03-26T16:43 - US-014
Session: iter-547e4870
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/prompt-observer.ts` with `handleBeforeAgentStart` and `handleAgentEnd`
- `handleBeforeAgentStart(event, ctx, project, baseDir?)`: records `user_prompt` observation with scrubbed prompt text from `event.prompt`, session, project fields, active_instincts from shared state
- `handleAgentEnd(event, ctx, project, baseDir?)`: records `agent_end` observation with session, project fields, active_instincts from shared state
- Self-observation guard (`shouldSkipObservation()`) checked as first action in both handlers
- `scrubSecrets()` applied to prompt text before writing
- Local `BeforeAgentStartEvent` and `AgentEndEvent` interfaces defined (portability pattern from US-013)
- 10 unit tests in `src/prompt-observer.test.ts`: prompt fields, scrubbing, active_instincts tagging, guard checks for both handlers
- All 170 tests pass, lint clean, typecheck clean
- Files changed: src/prompt-observer.ts, src/prompt-observer.test.ts
- **Learnings for future iterations:**
  - `handleBeforeAgentStart` returns void (observer only) - does not return `BeforeAgentStartEventResult`; US-022 (injector) is the handler that returns `{ systemPrompt: ... }`
  - Secret scrubbing test: use `Authorization: Bearer <token>` pattern, not `sk-` short keys (scrubber requires 32+ chars after `sk-`)
  - Pattern from US-013 applies here: spread `...buildActiveInstincts()` returns `{}` when empty, cleanly omitting the field
  - `_event` prefix for unused `AgentEndEvent` param avoids `noUnusedParameters` lint error
---

## 2026-03-26T16:45 - US-015
Session: iter-c0242176
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/prompts/analyzer-system.ts` with `buildAnalyzerSystemPrompt()` - pure template construction, no I/O
- 7 section builder functions (all under 50 lines): instinct file format, pattern detection, feedback analysis, confidence scoring, scope decision, conservativeness rules
- Prompt covers: exact YAML frontmatter spec, 5 pattern heuristics (corrections/errors/workflows/tool prefs/anti-patterns), active_instincts cross-reference for confirmed/contradicted/inactive feedback, discovery brackets (0.3/0.5/0.7/0.85) + feedback deltas (+0.05/-0.15) + passive decay (-0.02/week) + clamping [0.1-0.9], project vs global scope guide, 7 conservativeness rules (3+ obs min, no code snippets, etc.)
- 28 unit tests in `src/prompts/analyzer-system.test.ts` verifying all required sections
- All 198 tests pass, lint clean, typecheck clean
- Files changed: src/prompts/analyzer-system.ts, src/prompts/analyzer-system.test.ts
- **Learnings for future iterations:**
  - `beforeAll` must be explicitly imported from vitest (not in globals) - missing import causes `no-undef` lint error
  - Section builder pattern: split large prompt into small named functions, join with `\n` - keeps each under 50-line limit and makes sections independently testable
  - US-016 (user prompt) lives in `src/prompts/analyzer-user.ts` (same directory); US-015 is the system prompt counterpart
---

## 2026-03-26T16:47 - US-016
Session: iter-6a14b85c
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/prompts/analyzer-user.ts` with `buildAnalyzerUserPrompt()` and `tailObservations()` helper
- `tailObservations(observationsPath, maxEntries?)`: reads JSONL file, returns last `maxEntries` lines as string array (default 500); returns empty array if file does not exist; filters blank lines
- `buildAnalyzerUserPrompt(observationsPath, instinctsDir, project)`: builds user prompt with observation file path, instinct directory path, project_id, project_name, tailed observation content, and step-by-step instructions for the Haiku analyzer
- `MAX_TAIL_ENTRIES = 500` constant (no hardcoding)
- 13 unit tests in `src/prompts/analyzer-user.test.ts`: tail behavior (empty/below limit/at limit/blank lines/500-cap), prompt path inclusion, project context, tailed content, missing-file placeholder
- Fixed test import bug: `tmpdir` is from `node:os`, not `node:path` - caused TypeError that skipped all 13 tests
- All 211 tests pass (16 test files), lint clean, typecheck clean
- Files changed: src/prompts/analyzer-user.ts, src/prompts/analyzer-user.test.ts
- **Learnings for future iterations:**
  - `tmpdir` comes from `node:os`, not `node:path` - a common import confusion; always double-check
  - `tailObservations` is designed to be called inside `buildAnalyzerUserPrompt` but exported separately for testability and potential reuse by US-020 (timer management)
  - The prompt embeds observations inline so Haiku sees content in the user message (not just a file path to open), while also including the paths for reading/writing instinct files with the read/write tools
  - `Array.slice(-n)` returns the last n elements naturally - no manual index math needed
---

## 2026-03-26T16:52 - US-018
Session: iter-9de3fffb
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/analyzer-stream.ts` with `parseAnalyzerStream(stdout)` and `parseEventLine(line)` (exported for unit testing)
- `parseEventLine(line)`: trims whitespace, returns null for empty lines, catches JSON.parse errors with `console.warn`, returns `PiEvent | null`
- `parseAnalyzerStream(stdout)`: uses `readline.createInterface` over the Readable to iterate NDJSON lines; supports both `event` and `type` keys for event type (Pi version robustness); sets `success=true` on `agent_end`; tracks write tool `tool_execution_end` events for `filesWritten`; collects string/boolean `error` and `is_error` fields into `errors`
- `AnalysisResult` interface: `{ success: boolean; filesWritten: string[]; errors: string[] }`
- `extractWrittenFile()`: checks `args.path`, `args.file_path`, `args.filename` in priority order
- `isToolError()` / `buildErrorMessage()`: handles `error` (string or bool) and `is_error` (bool) variants
- All constants named (EVENT_TOOL_END, EVENT_AGENT_END, WRITE_TOOL_NAME) - no hardcoding
- 23 unit tests in `src/analyzer-stream.test.ts`: parseEventLine (4 tests), success detection (4), files written (7), error collection (5), malformed lines (2), combined result (1)
- All 257 tests pass (18 test files), lint clean, typecheck clean
- Files changed: src/analyzer-stream.ts, src/analyzer-stream.test.ts
- **Learnings for future iterations:**
  - `readline.createInterface({ input: stdout, crlfDelay: Infinity })` is the idiomatic way to iterate NDJSON from a stream - supports `for await` naturally
  - Test NDJSON streams with `Readable.from(lines.join("\n"))` - no temp files needed
  - `vi` must be explicitly imported from vitest when using `vi.spyOn` (not a global without vitest globals config)
  - Pi JSON mode may use `event` or `type` as the event key - support both for robustness
  - `parseEventLine` is exported separately to allow direct unit testing without stream setup overhead
  - Malformed line test: use `vi.spyOn(console, "warn").mockImplementation(...)` + `mockRestore()` to avoid polluting other test output
---

## 2026-03-26T17:00 - US-020
Session: iter-be710f63
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/analyzer-timer.ts` with `startAnalyzerTimer`, `stopAnalyzerTimer`, `getSkipReason`, and supporting helpers
- `startAnalyzerTimer(config, projectId, onAnalyze, baseDir?)`: clears any existing timer, sets `setInterval` at `run_interval_minutes * 60 * 1000` ms; each tick calls `getSkipReason` and only invokes `onAnalyze()` callback when null (no skip)
- `stopAnalyzerTimer()`: clears interval handle; called on `session_shutdown` (US-032 wiring)
- `getSkipReason(config, projectId, baseDir?)`: returns the first failing condition or null; check order: (1) `isAnalysisRunning()` -> in_progress, (2) `countObservations < min_observations_to_analyze` -> insufficient_observations, (3) `!isWithinActiveHours(...)` -> outside_active_hours, (4) idle > `max_idle_seconds` -> user_idle
- `countObservations(projectId, baseDir?)`: reads observations.jsonl, counts non-empty lines; returns 0 for missing file
- `getLastObservationTime(projectId, baseDir?)`: reads last line of observations.jsonl, parses `timestamp` field to epoch ms; returns null for missing file, empty file, or malformed JSON
- `isWithinActiveHours(startHour, endHour)`: uses `new Date().getHours()` (LOCAL time); range is `[start, end)` - end hour is exclusive
- `resetTimerState()` / `isTimerRunning()` exported for test isolation
- `isAnalysisRunning()` from analyzer-runner is mocked with `vi.mock("./analyzer-runner.js")` - use `vi.resetAllMocks()` + explicit `mockReturnValue(false)` in beforeEach (NOT `vi.clearAllMocks()`) to prevent mock state bleed between tests
- 27 unit tests in `src/analyzer-timer.test.ts` using `vi.useFakeTimers()` + `vi.setSystemTime()` for hour-based tests
- All 295 tests pass, lint clean, typecheck clean
- Files changed: src/analyzer-timer.ts, src/analyzer-timer.test.ts
- **Learnings for future iterations:**
  - `vi.clearAllMocks()` clears call counts but NOT mock implementations set via `mockReturnValue()` - use `vi.resetAllMocks()` + re-set defaults in `beforeEach` to prevent bleed between tests
  - `vi.setSystemTime(new Date(year, month, day, hour))` uses LOCAL time constructor - correct approach for testing `getHours()` without timezone issues (avoids UTC string parsing)
  - `active_hours_end: 24` effectively means "all day active" since `getHours()` returns 0-23, and `currentHour < 24` is always true
  - `onAnalyze` callback pattern enables US-032 to inject the full analysis execution without timer module knowing about prompt construction details
  - `isAnalysisRunning()` from analyzer-runner is the re-entrancy source of truth; timer does not maintain its own running flag
---

## Codebase Patterns
- **Observation JSONL**: `appendObservation(obs, projectId, baseDir?)` in `src/observations.ts`; `appendFileSync` for atomic line appends; archival uses `renameSync` (atomic move); `cleanOldArchives(projectId, baseDir?)` uses `utimesSync`-testable `statSync().mtimeMs` for age check; archive filenames use ISO timestamp with `:` and `.` replaced by `-`
- **Archive cleanup API**: `cleanOldArchives` is designed to be called once per `session_start` (US-032 wiring); uses `statSync().mtimeMs` for age comparison against `Date.now()`; safe to call on empty or missing dirs
- **Storage layout**: `ensureStorageLayout(project, baseDir?)` in `src/storage.ts`; accepts optional `baseDir` for testability (defaults to `~/.pi/continuous-learning/`); path helpers all accept optional `baseDir` param; `mkdirSync({ recursive: true })` handles idempotency for directories; `existsSync` guard before `writeFileSync` for project.json (only-once write)
- **Registry pattern**: `projects.json` is a `Record<string, ProjectEntry>` keyed by project ID; read with `JSON.parse(readFileSync(...))`, update with spread `{ ...existing, [id]: entry }`, write back atomically
- **testable path functions**: Export helpers like `getProjectDir(id, baseDir?)` with optional baseDir; in tests pass a real `mkdtempSync` temp dir - no fs mocking needed; use `afterAll` with `rmSync(dir, { recursive: true, force: true })` for cleanup
- **Project detection**: `detectProject(pi, cwd)` in `src/project.ts`; uses `pi.exec('git', ['remote', 'get-url', 'origin'], { cwd })` - passes `cwd` option so git runs in the correct directory; SHA256 hash via `node:crypto` `createHash('sha256').update(input).digest('hex').substring(0, 12)`; `GLOBAL_PROJECT_ID = 'global'` constant for non-git fallback
- **pi.exec mocking in tests**: create minimal mock `{ exec: vi.fn(...) } as unknown as ExtensionAPI`; chain `.mockResolvedValueOnce()` for sequential call behaviors; ExecResult shape: `{ code, stdout, stderr, killed }`
- **Config module**: `loadConfig()` in `src/config.ts`; imports `Config` type from `./types.js`; uses TypeBox `Type.Partial(Type.Object(...))` + `Value.Clean()` for runtime validation of partial overrides; `DEFAULT_CONFIG` and `CONFIG_PATH` are named exports
- **ESLint Node globals**: `eslint.config.js` imports `globals` from the `globals` package and spreads `globals.node` into `languageOptions.globals` to allow `console`, `Buffer`, `process`, etc.
- **fs mock pattern**: In vitest, `vi.mock("node:fs")` + `vi.mocked(fs).existsSync.mockReturnValue(...)` + cast mock return as `unknown as ReturnType<typeof fs.readFileSync>` to satisfy TypeScript

- **Pi Extension structure**: Default export `function(_pi: ExtensionAPI): void` in `src/index.ts`; pi-package requires `keywords: ["pi-package"]` and `pi.extensions` in package.json
- **Module resolution**: `"module": "NodeNext"` + `"moduleResolution": "NodeNext"` in tsconfig; use `.js` extension in relative imports (e.g., `import from "./index.js"`)
- **Peer deps available**: All `@mariozechner/*` packages (pi-coding-agent, pi-ai, pi-tui) and `@sinclair/typebox` are installed as peer deps via npm auto-install
- **ESLint flat config**: `eslint.config.js` imports `@eslint/js` and `@typescript-eslint/eslint-plugin` directly; `files: ["src/**/*.ts"]` pattern scopes TS rules
- **Run checks directly**: `node_modules/.bin/vitest run`, `node_modules/.bin/eslint src/`, `node_modules/.bin/tsc --noEmit` for raw output (pi output filter wraps npm scripts)
---

## 2026-03-26T16:33 - US-011
Session: iter-3701d32b
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/active-instincts.ts` - shared state module for active instinct ID tracking
- `getCurrentActiveInstincts()`: returns copy of current active instinct ID array (immutable return)
- `setCurrentActiveInstincts(ids)`: replaces state with copy of input array (immutable set)
- `clearActiveInstincts()`: resets state to empty array
- 6 unit tests in `src/active-instincts.test.ts`: default state, set/get, replace, clear, mutation isolation, empty set
- All 136 tests pass, lint clean, typecheck clean
- Files changed: src/active-instincts.ts, src/active-instincts.test.ts
- **Learnings for future iterations:**
  - Returns a copy (`[...activeInstincts]`) to enforce immutability - callers cannot mutate internal state
  - `clearActiveInstincts()` used by US-023 on `agent_end` to reset between prompts
  - `getCurrentActiveInstincts()` used by US-013/014 observers to populate `active_instincts` observation field
  - `beforeEach(() => clearActiveInstincts())` essential in tests - module state persists across test cases
---

## 2026-03-26T16:35 - US-012
Session: iter-141d8d4a
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/observations.ts` with `appendObservation` and `cleanOldArchives`
- `appendObservation(obs, projectId, baseDir?)`: checks file size via `statSync`, archives at >= 10MB with `renameSync`, then `appendFileSync` new line
- `cleanOldArchives(projectId, baseDir?)`: reads archive dir, checks `mtimeMs` of each `.jsonl` file, `unlinkSync` files >= 30 days old
- Archive filename: ISO timestamp with `:` and `.` replaced by `-` to be filesystem-safe
- `MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024` and `MAX_ARCHIVE_AGE_MS = 30 * 24 * 60 * 60 * 1000` as named constants (no hardcoding)
- 9 unit tests using real `mkdtempSync` temp dirs; `utimesSync` used to simulate old/new file mtime for archive age tests
- All 145 tests pass, lint clean, typecheck clean
- Files changed: src/observations.ts, src/observations.test.ts
- **Learnings for future iterations:**
  - `utimesSync(path, atime, mtime)` sets file access/modification times - use for testing time-based logic without mocking `Date.now()`
  - `renameSync` for archival is atomic on same filesystem (both paths under same temp dir in tests)
  - `cleanOldArchives` must be called from `session_start` handler in US-032 wiring
  - `appendObservation` is the primary entry point for US-013 and US-014 (tool call and user prompt observers)
---

## PRD Run Context
PRD Branch: ralph/continuous-learning
Started: 2026-03-26T14:00:28.554Z
---

## 2026-03-26T14:04 - US-001
Session: iter-44885990
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Scaffolded full pi-package project structure
- Created package.json with pi-package manifest, correct peerDependencies
- Created tsconfig.json with strict: true, NodeNext module resolution
- Created vitest.config.ts with node environment, 80% coverage thresholds
- Created eslint.config.js with @typescript-eslint rules
- Created src/index.ts as no-op default export accepting ExtensionAPI
- Created src/index.test.ts with 3 unit tests (all passing)
- Updated .gitignore to exclude node_modules/ and dist/
- All checks pass: vitest (3/3), eslint (0 issues), tsc (0 errors)
- Files changed: package.json, tsconfig.json, vitest.config.ts, eslint.config.js, src/index.ts, src/index.test.ts, .gitignore
- **Learnings for future iterations:**
  - Pi uses jiti to load extensions - NodeNext module resolution with .js imports required
  - peerDependencies are auto-installed by npm (all @mariozechner/* available in node_modules)
  - eslint.config.js flat config: import @eslint/js and @typescript-eslint explicitly
  - vitest globals: true enables describe/it/expect without imports (but explicit imports preferred for type safety)
  - pi output filter intercepts stdout/stderr - use node_modules/.bin/ direct calls to see raw output
  - tsconfig: noUnusedParameters allows _prefixed params (used in index.ts: _pi)
---

## 2026-03-26T14:06 - US-002
Session: iter-e616c616
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Defined all shared TypeScript interfaces in `src/types.ts`
- `ObservationEvent` union type: tool_start | tool_complete | user_prompt | agent_end
- `Observation` interface with all required fields (optional: tool, input, output, is_error, active_instincts)
- `Instinct` interface with all frontmatter fields including flagged_for_removal (needed by US-008/031)
- `InstinctScope` (project | global) and `InstinctSource` (personal | inherited) union types
- `ProjectEntry` interface: id, name, root, remote, created_at, last_seen
- `Config` interface with all spec defaults plus active_hours_start/end and max_idle_seconds (from US-030 spec)
- `src/types.test.ts` with 9 unit tests verifying all type exports are accessible
- All checks pass: vitest (12/12), eslint (0 issues), tsc (0 errors)
- Files changed: src/types.ts, src/types.test.ts
- **Learnings for future iterations:**
  - All downstream stories import from `./types.js` (NodeNext module resolution requires .js extension)
  - `flagged_for_removal` is optional on Instinct - set by confidence decay logic (US-008/031)
  - Config includes `active_hours_start`, `active_hours_end`, `max_idle_seconds` for US-030 (session guardian)
  - Type-only tests use `import type` - no runtime values needed, vitest still runs them fine
---

## 2026-03-26T14:08 - US-003
Session: iter-18ec9adf
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/config.ts` with `loadConfig()`, `DEFAULT_CONFIG`, and `CONFIG_PATH`
- TypeBox `PartialConfigSchema` validates/cleans user config at runtime boundary (only known fields pass through)
- Handles missing file (returns defaults), partial overrides (merge), invalid JSON (warn + return defaults)
- Default values match spec: run_interval_minutes:5, min_observations_to_analyze:20, min_confidence:0.5, max_instincts:20, model:claude-haiku-4-5, timeout_seconds:120
- Fixed eslint.config.js: added `globals.node` to languageOptions so console/Buffer/process are recognized
- 6 unit tests in `src/config.test.ts` covering all 4 acceptance scenarios + spec defaults + CONFIG_PATH
- All 18 tests pass, lint clean, typecheck clean
- Files changed: src/config.ts, src/config.test.ts, eslint.config.js
- **Learnings for future iterations:**
  - TypeBox `Value.Clean()` strips unknown properties from a parsed object - use for safe config/JSON ingestion at runtime boundaries
  - `vi.mock("node:fs")` mocks the entire fs module; call `vi.resetAllMocks()` in beforeEach to avoid test bleed
  - `fs.readFileSync` overloads - cast mock return value as `unknown as ReturnType<typeof fs.readFileSync>` to avoid Buffer type errors
  - `globals` package (already a transitive ESLint dep) provides Node.js/browser globals for flat config
---

## 2026-03-26T14:11 - US-004
Session: iter-897a907e
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/project.ts` with `detectProject(pi, cwd): Promise<ProjectEntry>`
- Resolution order: (1) git remote get-url origin -> hash URL, (2) git rev-parse --show-toplevel -> hash path, (3) fallback "global"
- Project ID = first 12 chars of SHA256 hash; project_name = basename(cwd)
- `HASH_LENGTH` and `GLOBAL_PROJECT_ID` constants in module (no hardcoding)
- 5 unit tests in `src/project.test.ts`: remote URL, no-remote path fallback, non-git global fallback, whitespace trimming for both paths
- All 23 tests pass, lint clean, typecheck clean
- Files changed: src/project.ts, src/project.test.ts
- **Learnings for future iterations:**
  - `pi.exec(cmd, args, options)` - must pass `{ cwd }` in options so git runs in the right directory
  - Mock `ExtensionAPI` as `{ exec: vi.fn() } as unknown as ExtensionAPI` - only mock the methods you call
  - Use `vi.fn().mockResolvedValueOnce()` chaining for multi-call sequential mock sequences
  - `node:crypto` `createHash` is available without any additional imports in Node
---

## 2026-03-26T14:14 - US-005
Session: iter-57f4e9b0
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/storage.ts` with `ensureStorageLayout(project, baseDir?)` and 7 path helper exports
- Creates global instincts/personal/ and instincts/inherited/ directories
- Creates projects/<id>/instincts/personal/, projects/<id>/instincts/inherited/, projects/<id>/observations.archive/
- Writes project.json only on first call (existsSync guard = idempotent)
- Always updates projects.json registry with current project entry (supports last_seen updates)
- All path helpers accept optional baseDir parameter for testability
- 15 unit tests use real mkdtempSync temp directories (no fs mocking needed)
- Tests cover: path helpers, directory creation, project.json no-overwrite, registry updates, multi-project registry, idempotency
- All 38 tests pass, lint clean, typecheck clean
- Files changed: src/storage.ts, src/storage.test.ts
- **Learnings for future iterations:**
  - `mkdirSync(dir, { recursive: true })` is naturally idempotent - no existsSync check needed for dirs
  - Inject baseDir as optional parameter instead of mocking node:os - avoids hoisting complications with vi.mock
  - TypeScript strict mode: `Record<string, T>` indexed access returns `T | undefined`; use non-null assertion `!` or add undefined check in tests
  - `rmSync(dir, { recursive: true, force: true })` in afterAll cleanly removes temp trees
---

## 2026-03-26T14:18 - US-006
Session: iter-4918b386
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/instinct-parser.ts` with `parseInstinct(content)` and `serializeInstinct(instinct)`
- Format: YAML frontmatter delimited by `---` with markdown body as the `action` text
- Used `yaml` package (transitive dep with built-in TS types) for parse/stringify
- ID validated as kebab-case with `/^[a-z0-9]+(-[a-z0-9]+)*$/`; throws on invalid
- Confidence clamped to 0.1-0.9 range in both parse and serialize (not thrown)
- Throws on missing required fields and malformed frontmatter delimiters
- Optional fields (project_id, project_name, evidence, flagged_for_removal) omitted when undefined
- 14 unit tests in `src/instinct-parser.test.ts`: valid parse, global/optional fields, clamping, id validation, round-trip
- All 52 tests pass, lint clean, typecheck clean
- Files changed: src/instinct-parser.ts, src/instinct-parser.test.ts
- **Learnings for future iterations:**
  - `yaml` package (transitive dep) provides `parse()` and `stringify()` with built-in TypeScript types - no @types needed
  - Regex `match[]` elements are `string | undefined` in TS even when match succeeds - use `?? ""` nullish coalescing
  - Instinct file format: frontmatter has all metadata fields; body is the `action` text
  - `splitFrontmatter()` uses regex `^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$` for robust CRLF handling
  - US-007 (CRUD operations) depends on this module for loadInstinct/saveInstinct
---

## 2026-03-26T16:24 - US-007
Session: iter-1ca7b7d9
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/instinct-store.ts` with 5 CRUD functions for instinct files
- `loadInstinct(filePath)`: reads .md and delegates to parseInstinct
- `saveInstinct(instinct, dir)`: validates ID, writes <dir>/<id>.md via serializeInstinct
- `listInstincts(dir)`: reads all .md files, silently skips malformed ones, returns empty array for missing dirs
- `loadProjectInstincts(projectId, baseDir?)`: loads from projects/<projectId>/instincts/personal/
- `loadGlobalInstincts(baseDir?)`: loads from instincts/personal/
- `assertNoPathTraversal()`: checks for `..`, `/`, `\` in instinct IDs before any file write
- 13 unit tests in `src/instinct-store.test.ts` using real mkdtempSync temp dirs (no fs mocking)
- Tests cover: load/save roundtrip, path traversal rejection (.. and /), list with malformed files, empty/missing dirs, project and global loaders
- All 65 tests pass, lint clean, typecheck clean
- Files changed: src/instinct-store.ts, src/instinct-store.test.ts
- **Learnings for future iterations:**
  - `listInstincts` silently skips malformed files - this is intentional so a bad instinct file doesn't break the session
  - `assertNoPathTraversal()` is a belt-and-suspenders guard; kebab-case validation in parseInstinct already prevents ".."
  - All path functions accept optional `baseDir` parameter for testability (no mocking needed)
  - Use `readdirSync(dir).filter(f => f.endsWith('.md'))` to enumerate instinct files
  - `existsSync(dir)` guard in `listInstincts` allows callers to call before storage layout is initialized
---

## 2026-03-26T16:27 - US-008
Session: iter-6fe3768b
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/confidence.ts` with 3 pure confidence scoring functions
- `initialConfidence(observationCount)`: bracket lookup returning 0.3/0.5/0.7/0.85
- `adjustConfidence(current, outcome)`: applies +0.05/−0.15/0 delta, clamps to [0.1, 0.9], flags if raw < 0.1
- `applyPassiveDecay(confidence, lastUpdated)`: −0.02/week based on elapsed ms, clamps, flags if raw < 0.1
- `ConfidenceResult` interface: `{ confidence: number; flaggedForRemoval: boolean }` for functions that can trigger removal
- `FeedbackOutcome` type: 'confirmed' | 'contradicted' | 'inactive'
- All constants named (CLAMP_MIN, CLAMP_MAX, DELTA_CONFIRMED, DECAY_PER_WEEK, etc.) - no hardcoded values
- 22 unit tests cover all brackets, outcomes, decay over time, boundary clamping, flagging
- Fixed 2 test bugs discovered during RED phase: (1) clamping test expected no-flag when raw=0.05 (should flag); (2) 100-day decay test was insufficient to hit floor (200 days needed for 0.5 start)
- All 87 tests pass, lint clean, typecheck clean
- Files changed: src/confidence.ts, src/confidence.test.ts
- **Learnings for future iterations:**
  - `toResult(raw)` helper pattern: check `raw < CLAMP_MIN` before clamping to determine flag - checking after clamping would always be false
  - `FeedbackOutcome` type exported from confidence.ts; downstream modules (US-031) will import it
  - US-021 (Instinct Loading and Filtering) directly depends on US-008 - it uses `flagged_for_removal` field populated by these functions
  - Decay math: need at least `(startConfidence - 0.1) / 0.02 * 7` days to hit the floor; for 0.5 start that's 140 days
---

## 2026-03-26T16:29 - US-009
Session: iter-ab4048d9
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/scrubber.ts` with `scrubSecrets(text): string` pure function
- Covers 9 regex patterns: authorization headers, standalone bearer, x-api-key/x-auth-token headers, api_key/access_key/secret_key fields, token fields, password/passwd/pwd, secret/credential/private_key, AWS AKIA keys, OpenAI/Anthropic sk- keys
- All patterns use `lastIndex = 0` reset before each `replace()` call (required for global regex reuse)
- `REDACTED = "[REDACTED]"` exported as named constant for downstream use
- 31 unit tests in `src/scrubber.test.ts`: authorization headers, HTTP headers, API key formats, token fields, password fields, credential fields, AWS keys, sk- keys, multi-secret strings, no-false-positive tests
- All 118 tests pass, lint clean, typecheck clean
- Files changed: src/scrubber.ts, src/scrubber.test.ts
- **Learnings for future iterations:**
  - Bearer token pattern must use `\S+` (not character class) to match values like `sk-abc...` that contain hyphens
  - Sequential pattern application can consume partial tokens leaving orphan suffixes - verify test edge cases where multiple patterns could match overlapping text
  - `pattern.lastIndex = 0` is required before each `replace()` when reusing global RegExp objects across calls
  - `scrubSecrets` is imported by US-013 (tool call observer) and US-014 (user prompt observer) - applied to input/output fields before writing
---

## 2026-03-26T16:31 - US-010
Session: iter-b0304039
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/observer-guard.ts` - shared state module for self-observation prevention
- `isAnalyzerRunning()` / `setAnalyzerRunning(value)`: module-level boolean flag getter/setter
- `shouldSkipPath(filePath)`: returns true for paths under `~/.pi/continuous-learning/` (exact match or prefix + separator check)
- `shouldSkipObservation(filePath?)`: combined guard - returns true if analyzer running OR path is filtered
- Fixed `startsWith` bug: used `path === BASE || path.startsWith(BASE + "/")` to prevent false matches on sibling dirs like `continuous-learning-other/`
- 12 unit tests in `src/observer-guard.test.ts`: flag lifecycle, path filtering edge cases, combined guard behavior
- All 130 tests pass, lint clean, typecheck clean
- Files changed: src/observer-guard.ts, src/observer-guard.test.ts
- **Learnings for future iterations:**
  - `shouldSkipObservation()` is the primary API for US-013/014 (tool call and prompt observers) - call it before any observation write
  - `setAnalyzerRunning(true)` must be called before analyzer subprocess starts and `setAnalyzerRunning(false)` after it completes (US-019)
  - Path prefix check: always append "/" separator when checking `startsWith` to avoid false positives on directory siblings
  - No TypeBox validation needed here - pure boolean/string state with no external input at type boundaries
---

## 2026-03-26T16:40 - US-013
Session: iter-d10957e7
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/tool-observer.ts` with `handleToolStart` and `handleToolEnd`
- `handleToolStart(event, ctx, project, baseDir?)`: records tool_start observation with tool name, truncated/scrubbed input (max 5000 chars), active_instincts, session, project fields
- `handleToolEnd(event, ctx, project, baseDir?)`: records tool_complete observation with tool name, truncated/scrubbed output (max 5000 chars), is_error, active_instincts
- Self-observation guard checked via `shouldSkipObservation()` as first action in both handlers
- `scrubSecrets()` applied to raw args/result before truncation
- `getCurrentActiveInstincts()` tags observations; omitted (not written as empty) when no instincts active
- Local `ToolExecutionStartEvent` and `ToolExecutionEndEvent` interfaces defined (local node_modules version of pi-coding-agent does not re-export these at top level - use local interfaces for portability)
- `getSessionId(ctx)` via `ctx.sessionManager.getSessionId()` (ReadonlySessionManager method)
- `lastObs()` helper in tests: reads observations and returns last one or throws - avoids TS18048 on `arr[arr.length - 1]` possibly-undefined
- 15 unit tests using real temp dirs; all 160 tests pass, lint clean, typecheck clean
- Files changed: src/tool-observer.ts, src/tool-observer.test.ts
- **Learnings for future iterations:**
  - Local `node_modules/@mariozechner/pi-coding-agent` top-level index.d.ts does NOT export `ToolExecutionStartEvent`/`ToolExecutionEndEvent` - import from `./dist/core/extensions/index.js` directly or define local interfaces
  - `ctx.sessionManager.getSessionId()` from `ReadonlySessionManager` - use this for session field
  - Spread `...buildActiveInstincts()` returns `{}` when empty - cleanly omits the field from the observation object
  - String args check (`typeof event.args === "string"`) prevents double-JSON-stringify for string inputs
  - `lastObs()` helper pattern: returns non-undefined or throws, avoids strict-mode TS array access errors
---

## 2026-03-26T16:49 - US-017
Session: iter-6ea40f72
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/analyzer-spawn.ts` with `spawnAnalyzer()` and `buildAnalyzerArgs()` (exported separately for testability)
- `buildAnalyzerArgs(systemPromptFile, userPrompt, model)`: constructs arg array with all required flags in spec order: --mode json, -p, --no-session, --tools read,write, --no-extensions, --no-skills, --no-prompt-templates, --no-themes, --model <model>, --append-system-prompt <file>, <userPrompt>
- `spawnAnalyzer(systemPromptFile, userPrompt, cwd, model?)`: spawns "pi" with stdio ['ignore', 'pipe', 'pipe'], returns SpawnAnalyzerHandle with { process, completion }
- `completion` is a Promise<number | null> that resolves when the child process emits 'close' - carries the exit code (null if killed via signal)
- Model defaults to DEFAULT_CONFIG.model ("claude-haiku-4-5") - no hardcoding
- `ANALYZER_FLAGS` const array (as const) holds all non-variable flags; spread into buildAnalyzerArgs
- 23 unit tests in `src/analyzer-spawn.test.ts` using vi.mock("node:child_process") and a fake EventEmitter-based ChildProcess
- Tests cover: all required flag presence, arg order, --model value, stdio tuple, cwd option, handle.process identity, completion promise resolution, null exit code, default/override model
- All 234 tests pass, lint clean, typecheck clean
- Files changed: src/analyzer-spawn.ts, src/analyzer-spawn.test.ts
- **Learnings for future iterations:**
  - `vi.mock("node:child_process")` then `await import(...)` pattern (dynamic import after mock registration) is required for top-level vi.mock to apply before module evaluation
  - Fake ChildProcess: `EventEmitter` with typed extra fields cast via `as unknown as ChildProcess` - minimal surface area needed for tests
  - `completion` promise listens to 'close' event (not 'exit') - 'close' fires after stdio streams close, which is the right signal for "process fully done"
  - `buildAnalyzerArgs` is exported separately so US-018 and US-019 can reuse/test the arg construction independently
  - `ANALYZER_FLAGS as const` ensures the spread produces a readonly tuple - prevents accidental mutation of the flag list
---

## 2026-03-26T16:55 - US-019
Session: iter-be214e8d
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/analyzer-runner.ts` - analyzer subprocess lifecycle management
- `runAnalysis(params)`: spawns analyzer with timeout, re-entrancy guard, and cooldown
  - Re-entrancy guard: `_isRunning` flag returns `{ skipped: true, skipReason: 'in_progress' }` on concurrent call
  - Cooldown guard: `ANALYSIS_COOLDOWN_MS = 60_000` enforces 60-second minimum between runs
  - Timeout: `setTimeout` arms after spawn; fires `child.kill('SIGTERM')` after `timeoutSeconds`
  - Calls `setAnalyzerRunning(true/false)` to suppress self-observation during analysis
  - `clearRunState()` helper resets all state in `finally` block (runs even on timeout)
- `shutdownAnalyzer()`: kills current process with SIGTERM, clears timeout handle
- `resetAnalyzerState()`: exported for test isolation only (resets all module-level state)
- `isAnalysisRunning()` / `getLastRunTime()`: exported state accessors for US-020
- `ANALYSIS_COOLDOWN_MS` exported for US-020 (timer management) to reference
- 11 unit tests in `src/analyzer-runner.test.ts` using `vi.useFakeTimers()`:
  - re-entrancy returns in_progress skip (process resolved via close event)
  - allows new run after previous completes (with cooldown advance)
  - cooldown blocks run within 60s, allows after 61s
  - timeout at 5s fires kill, resolves run, resets running flag
  - shutdown kills process immediately, does nothing when no process
  - setAnalyzerRunning called with true on start, false on end
  - isAnalysisRunning/getLastRunTime initial state and post-run values
- Files changed: src/analyzer-runner.ts, src/analyzer-runner.test.ts
- **Learnings for future iterations:**
  - `vi.useFakeTimers()` + `vi.advanceTimersByTime(ms)` is the clean approach for timeout tests - no actual waiting
  - `vi.mock("./module.js")` + `await import("./module.js")` pattern required for mocks to apply before module evaluation
  - Test deadlock risk: "never-resolving" streams must still resolve when process is killed - use `proc.on("close", () => resolve(...))` inside the mock implementation
  - `clearRunState()` in `finally` ensures cleanup even when stream parsing throws or process times out
  - Export `resetAnalyzerState()` for test isolation of module-level state (do not call in production)
  - `shutdownAnalyzer()` is the function for US-032 to wire to `session_shutdown` event handler
  - `isAnalysisRunning()` and `getLastRunTime()` are the accessors US-020 (timer management) will use to implement skip conditions
---
