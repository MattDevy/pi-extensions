## Codebase Patterns
- **Storage layout**: `ensureStorageLayout(project, baseDir?)` in `src/storage.ts`; accepts optional `baseDir` for testability (defaults to `~/.pi/continuous-learning/`); path helpers all accept optional `baseDir` param; `mkdirSync({ recursive: true })` handles idempotency for directories; `existsSync` guard before `writeFileSync` for project.json (only-once write)
- **Registry pattern**: `projects.json` is a `Record<string, ProjectEntry>` keyed by project ID; read with `JSON.parse(readFileSync(...))`, update with spread `{ ...existing, [id]: entry }`, write back atomically
- **testable path functions**: Export helpers like `getProjectDir(id, baseDir?)` with optional baseDir; in tests pass a real `mkdtempSync` temp dir - no fs mocking needed; use `afterAll` with `rmSync(dir, { recursive: true, force: true })` for cleanup
- **Project detection**: `detectProject(pi, cwd)` in `src/project.ts`; uses `pi.exec('git', ['remote', 'get-url', 'origin'], { cwd })` - passes `cwd` option so git runs in the correct directory; SHA256 hash via `node:crypto` `createHash('sha256').update(input).digest('hex').substring(0, 12)`; `GLOBAL_PROJECT_ID = 'global'` constant for non-git fallback
- **pi.exec mocking in tests**: create minimal mock `{ exec: vi.fn(...) } as unknown as ExtensionAPI`; chain `.mockResolvedValueOnce()` for sequential call behaviors; ExecResult shape: `{ code, stdout, stderr, killed }`
- **Config module**: `loadConfig()` in `src/config.ts`; imports `Config` type from `./types.js`; uses TypeBox `Type.Partial(Type.Object(...))` + `Value.Clean()` for runtime validation of partial overrides; `DEFAULT_CONFIG` and `CONFIG_PATH` are named exports
- **ESLint Node globals**: `eslint.config.js` imports `globals` from the `globals` package and spreads `globals.node` into `languageOptions.globals` to allow `console`, `Buffer`, `process`, etc.
- **fs mock pattern**: In vitest, `vi.mock("node:fs")` + `vi.mocked(fs).existsSync.mockReturnValue(...)` + cast mock return as `unknown as ReturnType<typeof fs.readFileSync>` to satisfy TypeScript

- **Pi Extension structure**: Default export `function(_pi: ExtensionAPI): void` in `src/index.ts`; pi-package requires `keywords: ["pi-package"]` and `pi.extensions` in package.json
- **Module resolution**: `"module": "NodeNext"` + `"moduleResolution": "NodeNext"` in tsconfig; use `.js` extension in relative imports (e.g., `import from "./index.js"`)
- **Peer deps available**: All `@mariozechner/*` packages (pi-coding-agent, pi-ai, pi-tui) and `@sinclair/typebox` are installed as peer deps via npm auto-install
- **ESLint flat config**: `eslint.config.js` imports `@eslint/js` and `@typescript-eslint/eslint-plugin` directly; `files: ["src/**/*.ts"]` pattern scopes TS rules
- **Run checks directly**: `node_modules/.bin/vitest run`, `node_modules/.bin/eslint src/`, `node_modules/.bin/tsc --noEmit` for raw output (pi output filter wraps npm scripts)
---

## PRD Run Context
PRD Branch: ralph/continuous-learning
Started: 2026-03-26T14:00:28.554Z
---

## 2026-03-26T14:04 - US-001
Session: iter-44885990
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Scaffolded full pi-package project structure
- Created package.json with pi-package manifest, correct peerDependencies
- Created tsconfig.json with strict: true, NodeNext module resolution
- Created vitest.config.ts with node environment, 80% coverage thresholds
- Created eslint.config.js with @typescript-eslint rules
- Created src/index.ts as no-op default export accepting ExtensionAPI
- Created src/index.test.ts with 3 unit tests (all passing)
- Updated .gitignore to exclude node_modules/ and dist/
- All checks pass: vitest (3/3), eslint (0 issues), tsc (0 errors)
- Files changed: package.json, tsconfig.json, vitest.config.ts, eslint.config.js, src/index.ts, src/index.test.ts, .gitignore
- **Learnings for future iterations:**
  - Pi uses jiti to load extensions - NodeNext module resolution with .js imports required
  - peerDependencies are auto-installed by npm (all @mariozechner/* available in node_modules)
  - eslint.config.js flat config: import @eslint/js and @typescript-eslint explicitly
  - vitest globals: true enables describe/it/expect without imports (but explicit imports preferred for type safety)
  - pi output filter intercepts stdout/stderr - use node_modules/.bin/ direct calls to see raw output
  - tsconfig: noUnusedParameters allows _prefixed params (used in index.ts: _pi)
---

## 2026-03-26T14:06 - US-002
Session: iter-e616c616
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Defined all shared TypeScript interfaces in `src/types.ts`
- `ObservationEvent` union type: tool_start | tool_complete | user_prompt | agent_end
- `Observation` interface with all required fields (optional: tool, input, output, is_error, active_instincts)
- `Instinct` interface with all frontmatter fields including flagged_for_removal (needed by US-008/031)
- `InstinctScope` (project | global) and `InstinctSource` (personal | inherited) union types
- `ProjectEntry` interface: id, name, root, remote, created_at, last_seen
- `Config` interface with all spec defaults plus active_hours_start/end and max_idle_seconds (from US-030 spec)
- `src/types.test.ts` with 9 unit tests verifying all type exports are accessible
- All checks pass: vitest (12/12), eslint (0 issues), tsc (0 errors)
- Files changed: src/types.ts, src/types.test.ts
- **Learnings for future iterations:**
  - All downstream stories import from `./types.js` (NodeNext module resolution requires .js extension)
  - `flagged_for_removal` is optional on Instinct - set by confidence decay logic (US-008/031)
  - Config includes `active_hours_start`, `active_hours_end`, `max_idle_seconds` for US-030 (session guardian)
  - Type-only tests use `import type` - no runtime values needed, vitest still runs them fine
---

## 2026-03-26T14:08 - US-003
Session: iter-18ec9adf
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/config.ts` with `loadConfig()`, `DEFAULT_CONFIG`, and `CONFIG_PATH`
- TypeBox `PartialConfigSchema` validates/cleans user config at runtime boundary (only known fields pass through)
- Handles missing file (returns defaults), partial overrides (merge), invalid JSON (warn + return defaults)
- Default values match spec: run_interval_minutes:5, min_observations_to_analyze:20, min_confidence:0.5, max_instincts:20, model:claude-haiku-4-5, timeout_seconds:120
- Fixed eslint.config.js: added `globals.node` to languageOptions so console/Buffer/process are recognized
- 6 unit tests in `src/config.test.ts` covering all 4 acceptance scenarios + spec defaults + CONFIG_PATH
- All 18 tests pass, lint clean, typecheck clean
- Files changed: src/config.ts, src/config.test.ts, eslint.config.js
- **Learnings for future iterations:**
  - TypeBox `Value.Clean()` strips unknown properties from a parsed object - use for safe config/JSON ingestion at runtime boundaries
  - `vi.mock("node:fs")` mocks the entire fs module; call `vi.resetAllMocks()` in beforeEach to avoid test bleed
  - `fs.readFileSync` overloads - cast mock return value as `unknown as ReturnType<typeof fs.readFileSync>` to avoid Buffer type errors
  - `globals` package (already a transitive ESLint dep) provides Node.js/browser globals for flat config
---

## 2026-03-26T14:11 - US-004
Session: iter-897a907e
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/project.ts` with `detectProject(pi, cwd): Promise<ProjectEntry>`
- Resolution order: (1) git remote get-url origin -> hash URL, (2) git rev-parse --show-toplevel -> hash path, (3) fallback "global"
- Project ID = first 12 chars of SHA256 hash; project_name = basename(cwd)
- `HASH_LENGTH` and `GLOBAL_PROJECT_ID` constants in module (no hardcoding)
- 5 unit tests in `src/project.test.ts`: remote URL, no-remote path fallback, non-git global fallback, whitespace trimming for both paths
- All 23 tests pass, lint clean, typecheck clean
- Files changed: src/project.ts, src/project.test.ts
- **Learnings for future iterations:**
  - `pi.exec(cmd, args, options)` - must pass `{ cwd }` in options so git runs in the right directory
  - Mock `ExtensionAPI` as `{ exec: vi.fn() } as unknown as ExtensionAPI` - only mock the methods you call
  - Use `vi.fn().mockResolvedValueOnce()` chaining for multi-call sequential mock sequences
  - `node:crypto` `createHash` is available without any additional imports in Node
---

## 2026-03-26T14:14 - US-005
Session: iter-57f4e9b0
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/storage.ts` with `ensureStorageLayout(project, baseDir?)` and 7 path helper exports
- Creates global instincts/personal/ and instincts/inherited/ directories
- Creates projects/<id>/instincts/personal/, projects/<id>/instincts/inherited/, projects/<id>/observations.archive/
- Writes project.json only on first call (existsSync guard = idempotent)
- Always updates projects.json registry with current project entry (supports last_seen updates)
- All path helpers accept optional baseDir parameter for testability
- 15 unit tests use real mkdtempSync temp directories (no fs mocking needed)
- Tests cover: path helpers, directory creation, project.json no-overwrite, registry updates, multi-project registry, idempotency
- All 38 tests pass, lint clean, typecheck clean
- Files changed: src/storage.ts, src/storage.test.ts
- **Learnings for future iterations:**
  - `mkdirSync(dir, { recursive: true })` is naturally idempotent - no existsSync check needed for dirs
  - Inject baseDir as optional parameter instead of mocking node:os - avoids hoisting complications with vi.mock
  - TypeScript strict mode: `Record<string, T>` indexed access returns `T | undefined`; use non-null assertion `!` or add undefined check in tests
  - `rmSync(dir, { recursive: true, force: true })` in afterAll cleanly removes temp trees
---

## 2026-03-26T14:18 - US-006
Session: iter-4918b386
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/instinct-parser.ts` with `parseInstinct(content)` and `serializeInstinct(instinct)`
- Format: YAML frontmatter delimited by `---` with markdown body as the `action` text
- Used `yaml` package (transitive dep with built-in TS types) for parse/stringify
- ID validated as kebab-case with `/^[a-z0-9]+(-[a-z0-9]+)*$/`; throws on invalid
- Confidence clamped to 0.1-0.9 range in both parse and serialize (not thrown)
- Throws on missing required fields and malformed frontmatter delimiters
- Optional fields (project_id, project_name, evidence, flagged_for_removal) omitted when undefined
- 14 unit tests in `src/instinct-parser.test.ts`: valid parse, global/optional fields, clamping, id validation, round-trip
- All 52 tests pass, lint clean, typecheck clean
- Files changed: src/instinct-parser.ts, src/instinct-parser.test.ts
- **Learnings for future iterations:**
  - `yaml` package (transitive dep) provides `parse()` and `stringify()` with built-in TypeScript types - no @types needed
  - Regex `match[]` elements are `string | undefined` in TS even when match succeeds - use `?? ""` nullish coalescing
  - Instinct file format: frontmatter has all metadata fields; body is the `action` text
  - `splitFrontmatter()` uses regex `^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$` for robust CRLF handling
  - US-007 (CRUD operations) depends on this module for loadInstinct/saveInstinct
---

## 2026-03-26T16:24 - US-007
Session: iter-1ca7b7d9
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/instinct-store.ts` with 5 CRUD functions for instinct files
- `loadInstinct(filePath)`: reads .md and delegates to parseInstinct
- `saveInstinct(instinct, dir)`: validates ID, writes <dir>/<id>.md via serializeInstinct
- `listInstincts(dir)`: reads all .md files, silently skips malformed ones, returns empty array for missing dirs
- `loadProjectInstincts(projectId, baseDir?)`: loads from projects/<projectId>/instincts/personal/
- `loadGlobalInstincts(baseDir?)`: loads from instincts/personal/
- `assertNoPathTraversal()`: checks for `..`, `/`, `\` in instinct IDs before any file write
- 13 unit tests in `src/instinct-store.test.ts` using real mkdtempSync temp dirs (no fs mocking)
- Tests cover: load/save roundtrip, path traversal rejection (.. and /), list with malformed files, empty/missing dirs, project and global loaders
- All 65 tests pass, lint clean, typecheck clean
- Files changed: src/instinct-store.ts, src/instinct-store.test.ts
- **Learnings for future iterations:**
  - `listInstincts` silently skips malformed files - this is intentional so a bad instinct file doesn't break the session
  - `assertNoPathTraversal()` is a belt-and-suspenders guard; kebab-case validation in parseInstinct already prevents ".."
  - All path functions accept optional `baseDir` parameter for testability (no mocking needed)
  - Use `readdirSync(dir).filter(f => f.endsWith('.md'))` to enumerate instinct files
  - `existsSync(dir)` guard in `listInstincts` allows callers to call before storage layout is initialized
---
