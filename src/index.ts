import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

/**
 * Pi Continuous Learning Extension
 *
 * Observes coding sessions, records events, and uses a background
 * Haiku process to distill observations into reusable instincts.
 */
export default function (_pi: ExtensionAPI): void {
	// No-op placeholder - full implementation wired in US-032
}
